# SDR and signal processing
numpy
scipy
sounddevice

# Transcription
mlx-whisper

# Dispatch
openai
python-dotenv

# Note: SoapySDR must be installed via system package manager:
#   brew install soapysdr soapyhackrf hackrf  (macOS)
#   apt install python3-soapysdr soapysdr-module-hackrf  (Debian/Ubuntu)
